<div class="span6">
  <div class="content">

    <div class="module">
      <div class="module-head">
        <h2>
          <%= "Set Section" %>

        </h2>
      </div>


      <div class="module-body ">

        <%= form_for(@set_section) do |f| %>
            <% if @set_section.errors.any? %>
                <div id="error_explanation">
                  <h2><%= pluralize(@set_section.errors.count, "error") %> prohibited this set_section from being
                    saved:</h2>

                  <ul>
                    <% @set_section.errors.full_messages.each do |message| %>
                        <li><%= message %></li>
                    <% end %>
                  </ul>
                </div>
            <% end %>

            <div class="field">
              <%= f.label :code %><br>
              <%= f.text_field :code %>
            </div>
            <div class="field">
              <%= f.label :name %><br>
              <%= f.text_field :name %>
            </div>
            <div class="field">
              <%= f.label :display %><br>
              <%= f.text_field :display %>
            </div>
            <div class="module-option clearfix">

              <div class="pull-right">

                <% if @set_section.new_record? %>

                    <%= button_to "Create Section", {action: "create"}, class: "btn btn-primary" %>
                <% elsif @set_section.persisted? %>

                    <%= button_to "Update Section", {action: "update"}, class: "btn btn-primary" %>


                <% end %>

              </div>
            </div> <!-- clearfix-->
            </div>
        <% end %>
        </div>
  </div>
</div>