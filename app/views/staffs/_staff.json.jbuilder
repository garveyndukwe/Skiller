json.extract! staff, :id, :file_no, :last_name, :first_name, :middle_name, :rank, :gender, :qualification, :area_of_specialization, :station, :department, :section, :grade, :step, :submitted, :created_at, :updated_at
json.url staff_url(staff, format: :json)
